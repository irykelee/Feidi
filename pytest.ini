[pytest]
# Minimal config so `pytest` (if installed) can run the unittest suites.
# The canonical runner is tests/run_tests.sh (stdlib, no deps).
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
